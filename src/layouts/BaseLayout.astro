---
const {
  title = "香港共和國",
  description = "香港共和國政府官方網站",
} = Astro.props;
---

<!DOCTYPE html>
<html lang="zh-Hant-HK">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title>{title}</title>
    <meta name="description" content={description} />

    <meta name="author" content="香港共和國政府" />
    <meta name="theme-color" content="#b80000" />

    <!-- Open Graph -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />

    <link rel="icon" href="/HKR/favicon.svg" />
  </head>

  <body>
    <!-- Topbar -->
    <div class="topbar">
      <div class="container topbar-inner">
        <div class="topbar-left">
          <span class="dot" aria-hidden="true"></span>
          <span class="topbar-text">香港共和國政府 · 官方門戶</span>
        </div>
        <div class="topbar-right">
          <span class="topbar-meta">僅供官方發布與公共資訊查閱</span>
        </div>
      </div>
    </div>

    <!-- Header -->
    <header class="site-header">
      <div class="container header-inner">
        <a class="brand" href="/HKR/" aria-label="返回首頁">
          <img
            src="/HKR/assets/emblem.svg"
            alt="香港共和國國徽"
            class="brand-emblem"
            loading="eager"
          />
          <div class="brand-text">
            <div class="brand-title">香港共和國</div>
            <div class="brand-subtitle">政府官方網站</div>
          </div>
        </a>

        <nav class="main-nav" aria-label="主要導覽">
          <a href="/HKR/">首頁</a>
          <a href="/HKR/government">政府</a>
          <a href="/HKR/laws">法律</a>
          <a href="/HKR/timeline">年表</a>
          <a href="/HKR/press">公告</a>
          <a href="/HKR/symbols">國家象徵</a>
        </nav>
      </div>
    </header>

    <!-- Main -->
    <main class="site-main">
      <div class="container">
        <slot />
      </div>
    </main>

    <!-- Footer -->
    <footer class="site-footer">
      <div class="container footer-inner">
        <div class="footer-row">
          <div class="footer-brand">
            <img
              src="/HKR/assets/emblem.svg"
              alt="香港共和國國徽"
              class="footer-emblem"
              loading="lazy"
            />
            <div>
              <div class="footer-title">香港共和國政府</div>
              <div class="footer-sub">官方公共資訊與公告發布平台</div>
            </div>
          </div>

          <div class="footer-meta">
            <div>© 2043–現在 香港共和國政府</div>
            <div class="footer-note">
              本網站所載內容屬官方公開發布資訊。未經授權不得擅自更改、剪裁或曲解。
            </div>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>

<style>
  :root {
    --hkr-red: #b80000;
    --ink: #111;
    --muted: #5b5b5b;
    --bg: #ffffff;
    --paper: #fafafa;
    --border: #e7e7e7;
    --shadow: 0 6px 20px rgba(0, 0, 0, 0.06);
  }

  * {
    box-sizing: border-box;
  }

  body {
    margin: 0;
    font-family: -apple-system, BlinkMacSystemFont, "PingFang TC",
      "Noto Sans TC", "Microsoft JhengHei", "Segoe UI", Roboto, Helvetica, Arial,
      sans-serif;
    color: var(--ink);
    background: var(--bg);
    line-height: 1.7;
    letter-spacing: 0.2px;
  }

  .container {
    max-width: 1120px;
    margin: 0 auto;
    padding: 0 1.25rem;
  }

  /* Topbar */
  .topbar {
    background: #111;
    color: #f2f2f2;
    font-size: 0.82rem;
  }

  .topbar-inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.55rem 0;
    gap: 1rem;
  }

  .topbar-left {
    display: flex;
    align-items: center;
    gap: 0.55rem;
  }

  .dot {
    width: 8px;
    height: 8px;
    border-radius: 99px;
    background: var(--hkr-red);
    display: inline-block;
  }

  .topbar-meta {
    color: #cfcfcf;
    white-space: nowrap;
  }

  /* Header */
  .site-header {
    background: #fff;
    border-bottom: 1px solid var(--border);
    position: sticky;
    top: 0;
    z-index: 50;
  }

  .header-inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0.9rem 0;
    gap: 1rem;
  }

  .brand {
    display: flex;
    align-items: center;
    gap: 0.85rem;
    text-decoration: none;
    color: var(--ink);
    min-width: 240px;
  }

  .brand-emblem {
    width: 44px;
    height: 44px;
    flex: 0 0 auto;
  }

  .brand-title {
    font-weight: 750;
    letter-spacing: 0.6px;
  }

  .brand-subtitle {
    font-size: 0.86rem;
    color: var(--muted);
    margin-top: 0.05rem;
  }

  .main-nav {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: 0.75rem 1rem;
  }

  .main-nav a {
    text-decoration: none;
    color: var(--ink);
    font-size: 0.95rem;
    padding: 0.45rem 0.6rem;
    border-radius: 10px;
  }

  .main-nav a:hover {
    background: #f3f3f3;
    color: var(--hkr-red);
  }

  /* Main */
  .site-main {
    padding: 2.2rem 0 3.2rem;
  }

  /* Global typography polish (affects all pages) */
  h1 {
    font-size: 2.35rem;
    line-height: 1.18;
    margin: 0 0 1rem 0;
    letter-spacing: 0.2px;
  }

  h2 {
    font-size: 1.35rem;
    margin: 2.25rem 0 0.75rem 0;
  }

  p {
    margin: 0.55rem 0;
    color: #222;
  }

  a {
    color: var(--hkr-red);
  }

  /* Footer */
  .site-footer {
    background: var(--paper);
    border-top: 1px solid var(--border);
  }

  .footer-inner {
    padding: 1.6rem 0;
    color: var(--muted);
    font-size: 0.9rem;
  }

  .footer-row {
    display: flex;
    justify-content: space-between;
    gap: 1.25rem;
    align-items: flex-start;
    flex-wrap: wrap;
  }

  .footer-brand {
    display: flex;
    align-items: center;
    gap: 0.9rem;
  }

  .footer-emblem {
    width: 54px;
    height: 54px;
    display: block;
  }

  .footer-title {
    font-weight: 700;
    color: #333;
  }

  .footer-sub {
    margin-top: 0.2rem;
    color: var(--muted);
  }

  .footer-note {
    margin-top: 0.4rem;
    max-width: 520px;
  }

  @media (max-width: 860px) {
    .topbar-right {
      display: none;
    }
    .header-inner {
      align-items: flex-start;
      flex-direction: column;
    }
    .main-nav {
      justify-content: flex-start;
    }
  }
</style>
